Build

1. Install node/npm - e.g. brew install npm
2. Install typescript: npm install -g typescript

VS Code
Execute Run Build Task (⇧⌘B) from the global Terminal menu
tsc: build or tsc: watch


CVS
To test the extension, a command line CVS is needed
brew install cvs

# There was no way to contact you. Since this is the only way for CVS integration in VSCODE (which i desperately need :( ), please kindly explain hwo to integrate your CVS extenesion to vscode in ubuntu. Mail :: saha.saorav@gmail.com
